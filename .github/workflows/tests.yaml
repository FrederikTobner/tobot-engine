name: Tests

on:
  pull_request:
    branches: [ "master" ]

jobs:
  test:
    runs-on: ${{matrix.operatingsystems}}
    env:
      BUILD_TYPE: ${{matrix.buildtypes}}

    strategy:
      matrix:
        operatingsystems: [windows-latest] 
        buildtypes: [Release] 

    steps:
    - uses: actions/checkout@v3

    - name: Configure CMake
      run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} -DBUILD_SDL=OFF

    - name: Building Tests
      run: cmake --build ${{github.workspace}}/build --config ${{env.BUILD_TYPE}} --target tobot-data-structures_TESTS tobot-lang_TESTS tobot-math_TESTS tobot-test_TESTS

    - name: Running tests
      run: cd build && ctest -C ${{env.BUILD_TYPE}} 
